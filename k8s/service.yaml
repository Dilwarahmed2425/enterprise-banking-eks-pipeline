apiVersion: v1
kind: Service
metadata:
  name: banking-core-service
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: banking-core
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080